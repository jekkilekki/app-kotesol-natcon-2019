import data from '../utils/LocationsList.json'

// export default () => data

const INITIAL_STATE = {
  data
}

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default: 
      return state
  }
}